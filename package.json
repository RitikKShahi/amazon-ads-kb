{
  "name": "amazon-ads-kb",
  "version": "0.1.0",
  "private": true,
  "description": "Autonomous knowledge acquisition system for Amazon Ads — OKF v0.1 bundle",
  "scripts": {
    "test": "node --test 'tests/*.test.js'",
    "prefetch": "node scripts/prefetch_static_sources.js"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
